# speciai.team 네이버 블로그

블로그: **https://blog.naver.com/speciai_**

검색에서 걸리는 글을 만들어 발행하고, 실제로 몇 위에 걸렸는지 추적합니다.
소재 발굴 → 경쟁 분석 → 글 → 이미지 → 발행 → 순위 측정까지 한 폴더 안에 있습니다.

---

## 글 한 편 내는 순서

```bash
cd ~/Work/speciai/speciai.team/blog

# ① 뭘 쓸지 고른다 — 오래된 글이 상위를 지키는 키워드가 기회입니다
open research/전략.md

# ② 초고를 쓴다 (웹 검색으로 현행 법령을 확인합니다. 3~5분)
node generate.mjs "접대비 한도" --cat 세무

# ③ 본문 이미지를 만든다 (마커 자리마다 카드 20장 + 발행본)
node cards.mjs posts/2026-08-04-접대비-한도.md

#    한 주제를 여러 편으로 썼다면 서로 묶습니다
#    node series.mjs 취업규칙 posts/A.md posts/B.md posts/C.md

# ④ 원장에 등록한다
node rank.mjs --add posts/2026-08-04-접대비-한도.md

#    ─── 여기서 사람이 검토하고 네이버에 발행합니다 ───

# ⑤ 발행 주소를 원장에 알린다
node rank.mjs --publish 2026-08-04-접대비-한도 https://blog.naver.com/speciai_/223...
```

⑤까지 하면 **매일 아침 9시 30분에 순위가 자동으로 측정**됩니다.

---

## 발행 전에 반드시

**초고는 초고입니다.** 법률·세무 글이라 틀린 수치 하나가 신뢰를 깎습니다.

1. 글 맨 위 `checks:` 항목을 전문가가 확인합니다
2. 본문에 `[확인 필요: ~]` 가 남아 있으면 채우거나 문장을 들어냅니다
3. 조문 번호와 금액이 현행인지 대조합니다 — 세법·노동법은 매년 바뀝니다

발행을 자동화하지 않은 이유가 이것입니다.

---

## 파일 지도

### 글 만들기

| 파일 | 하는 일 |
|---|---|
| `generate.mjs` | 주제 → 초고. 웹 검색으로 현행 법령을 확인하고 규격에 맞춰 씁니다 |
| `cards.mjs` | 본문 마커 → 정사각 카드 이미지 + 발행본 |
| `series.mjs` | 같은 주제 글끼리 서로 링크로 묶기 |
| `posts/` | 생성된 글. `*.발행본.md` 는 이미지 경로가 꽂힌 것 |

### 블로그 꾸미기

| 파일 | 하는 일 |
|---|---|
| `render.mjs` | 타이틀·프로필·위젯·배경 등 이미지 10종 |
| `pdf.mjs` | 세팅 가이드 PDF (A4 6쪽) |
| `네이버블로그-세팅과-이미지규격.md` | 규격표와 세팅 순서 |
| `out/` | 렌더 결과 |

### 조사하기

| 파일 | 하는 일 |
|---|---|
| `research/topics.mjs` | 소재 발굴. 네이버 자동완성에서 **실제 검색어**를 긁습니다 |
| `research/study.mjs` | 키워드별 상위 10건 수집 → 경쟁도 |
| `research/extract.mjs` | 상위 글 본문을 구조 그대로 추출 |
| `research/글쓰기-규칙.md` | 상위 174건에서 뽑은 규격. **`generate.mjs` 의 시스템 프롬프트입니다** |
| `research/전략.md` | 어느 키워드부터 칠지 |

### 측정하기

| 파일 | 하는 일 |
|---|---|
| `rank.mjs` | 원장 등록·발행 확정·순위 측정 |
| `notify.mjs` | 변동이 있을 때만 디스코드로 보고 |
| `ledger.json` | 발행 이력과 순위 추이 |

---

## 자동으로 도는 것

| 시각 | 무엇 |
|---|---|
| 매일 09:30 | 순위 측정 → 변동 있으면 디스코드 |
| 일요일 10:00 | 경쟁 리서치 재수집 → 기회 키워드 보고 |

`crontab -l` 로 확인합니다. **글 생성과 발행은 자동으로 돌지 않습니다.**

---

## 사람이 해야 하는 것

| 주기 | 무엇 | 왜 |
|---|---|---|
| 글마다 | 발행 전 검토 | 법률·세무 정확성 |
| 글마다 | 네이버에 발행 + `--publish` | 로그인이 필요합니다 |
| 월 1회 | 조회수를 `ledger.json` 의 `views` 에 입력 | 블로그 통계는 API 가 없습니다 |

---

## 알아둘 것

- **디스코드 웹훅 URL 이 아직 없습니다.** `blog/.env` 에 `DISCORD_WEBHOOK_URL=` 을 넣기 전까지
  알림은 콘솔 미리보기만 합니다. cron 은 그래도 정상 동작합니다.
- **crontab 은 `crontab -` 로만 고칩니다.** `crontab 파일경로` 는 기존 것을 조용히 다 지웁니다.
  카드뉴스·정부지원사업 등 다른 작업이 같이 등록돼 있습니다.
- **네이버 요청 간격을 줄이지 마세요.** `study.mjs` 의 `GAP`(0.6초)과 `TOP_N`(10)은
  남의 서버에 부담을 주지 않는 선입니다.
- **카드 렌더는 `blog/` 안에서 따로 합니다.** `studio/카드뉴스/cardnews-white` 는 인스타 세로형
  전용이고 crontab 이 도는 운영 코드라 건드리지 않습니다.
- **블로그 자동화가 하나 더 있습니다.** `_공용/content-pipeline/post-generator/blog-daily.mjs` 가
  월·수·금 아침에 **뉴스 소재로** 5계정 초안을 만듭니다. 이쪽은 **검색 유입**이 목적이라 축이 다릅니다.

---

## 다시 학습시키기

상위 노출 기준이 바뀌었다고 느껴지면 순서대로 돌립니다.

```bash
cd research
node topics.mjs      # 소재 재발굴 (실검색어)
node study.mjs       # 경쟁도 재측정
node extract.mjs     # 상위 글 뼈대 재추출
```

그 다음 `글쓰기-규칙.md` 를 새 데이터에 맞게 고칩니다. 이 문서가 곧 생성 품질입니다.
