# speciai.team 네이버 블로그 — 세팅 순서와 이미지 규격

작성 2026-08-04 · 대상 서비스 `speciai.team` (본체)
이미지 원본은 `blog/out/`, 다시 뽑으려면 `node render.mjs`

---

## 1. 기본 세팅

블로그는 이미 열려 있습니다 — **https://blog.naver.com/speciai_**
남은 건 꾸미기 설정입니다. 네이버 로그인이 필요해서 이 단계는 직접 하셔야 합니다.

1. ~~블로그 만들기~~ — **완료.** 주소 `speciai_` 로 확정됐습니다.
2. **기본 정보** — 관리 → 기본 설정 → 기본 정보
   - 블로그명: `speciai.team | 중소기업 법률·세무·노무`
   - 별명: `speciai`
   - 소개글: 검색 결과에 같이 노출되니 키워드를 넣습니다.
     > 중소기업·스타트업이 놓치기 쉬운 법률·세무·노무 규정을 AI 어드바이저리가 먼저 짚어드립니다.
3. **레이아웃** — 관리 → 꾸미기 설정 → 레이아웃·위젯 설정 → **2단 (사이드바 우측)** 권장. 위젯 배너를 걸 자리가 생깁니다.
4. **스킨** — 같은 메뉴의 세부 디자인 설정에서 타이틀·배경·글영역을 각각 직접등록으로 바꿉니다.
5. **위젯** — 레이아웃·위젯 설정 → 위젯 직접등록. HTML 코드는 아래 4번 항목에 있습니다.
6. **카테고리** — 법률 / 세무 / 노무 / 컴플라이언스 사례 / 공지 5개로 시작하고, 글이 쌓이면 쪼갭니다.

> 주소 끝의 언더스코어(`speciai_`)는 사람이 따라 치기 어렵습니다.
> 명함·메일 서명에는 **주소를 직접 노출하지 말고** 「블로그」 텍스트에 링크를 거는 편이 안전합니다.

---

## 2. 이미지 규격 한눈에

네이버가 정한 고정값입니다. **가로는 반드시 맞추고, 세로만 조절**하시면 됩니다.

| 쓰는 곳 | 규격 (px) | 준비된 파일 | 비고 |
|---|---|---|---|
| 블로그 타이틀 | **966 × 50~600** | `01_타이틀_966x300.png` | 기본 폭. 300이 무난합니다 |
| 타이틀 (와이드 스킨) | **1920 × 50~600** | `02_타이틀_와이드_1920x300.png` | 와이드 레이아웃일 때만 |
| 프로필 이미지 | **161 × 161~286** | `03_프로필_161x161.png` | 정사각이 안전합니다 |
| 위젯 배너 | **170 × 자유** | `04~06_위젯_*.png` | 가로 170 고정. 넘으면 잘립니다 |
| 스킨 배경 | **1920 × 1080** | `09_배경_1920x1080.png` | 반복 없이 한 장으로 |
| 스킨 배경 (반복용) | **400 × 400** | `10_배경타일_400x400.png` | 상하좌우 반복 설정할 때 |
| 모바일 앱 커버 | **1080 × 1300** | `07_모바일커버_1080x1300.png` | 모바일 앱에서만 보입니다 |
| 포스팅 대표 썸네일 | **1300 × 885** | `08_썸네일템플릿_1300x885.png` | 검색 결과에 뜨는 이미지 |
| 본문 삽입 사진 | 가로 **966** 이하 | — | 넘으면 자동 축소돼 흐려집니다 |
| 카드뉴스 | **1300 × 1300** | 카드뉴스 파이프라인 재사용 | `studio/카드뉴스/cardnews-white` |

**공통 규칙**

- 포맷은 **PNG** (글자가 있는 이미지), 사진은 **JPG 85%**.
- 한 장당 **1MB 이하**로 맞추면 로딩이 눈에 띄게 빨라집니다. 배경은 특히 그렇습니다.
- 레티나 대응한다고 2배로 올리지 마세요. 네이버가 다시 압축해서 오히려 뭉갭니다.

---

## 3. 뒷 배경 이미지 — 여기가 제일 많이 틀립니다

배경은 **글 뒤에 깔리는 이미지**라 규격보다 밝기가 중요합니다.

**규격**
- 한 장으로 채울 때: `1920 × 1080`. 화면이 더 크면 가장자리가 잘리니 **중요한 요소를 중앙 966px 안에** 둡니다.
- 반복(타일)으로 채울 때: `400 × 400` 이하. 이음매가 보이면 안 되므로 패턴만 넣습니다. 준비한 타일은 격자 패턴이라 이어 붙어도 티가 안 납니다.
- 배경은 **고정(fixed)** 으로 두면 스크롤할 때 글만 움직여서 훨씬 깔끔합니다.

**밝기 기준**
- 글영역을 흰 배경으로 쓸 거라면 배경은 어두워도 됩니다. 지금 만든 게 그 조합입니다.
- 글영역을 투명하게 쓸 거라면 배경 대비를 **명도차 15% 이내**로 낮춰야 글이 읽힙니다.
- 사진을 배경으로 쓰신다면 **위에 검정 60% 오버레이**를 깔고 쓰세요. 그냥 깔면 본문이 안 읽힙니다.

준비된 `09_배경_1920x1080.png` 는 순흑 바탕에 오렌지 격자를 **투명도 4%** 로만 얹었습니다. 눈에 거의 안 보이는 게 정상이고, 그래야 글이 삽니다.

---

## 4. 위젯 배너 등록

관리 → 꾸미기 설정 → 레이아웃·위젯 설정 → **위젯 직접등록** → 아래 HTML을 붙여넣습니다.
`이미지주소` 자리에는 블로그에 이미지를 한 번 올린 뒤 나오는 URL을 넣으시면 됩니다.

```html
<a href="https://speciai.team/consult" target="_blank">
  <img src="이미지주소/04_위젯_무료진단_170x74.png" width="170" alt="무료 컴플라이언스 진단" />
</a>
```

위젯 3개를 각각 등록하고 링크를 이렇게 연결합니다.

| 배너 | 연결할 곳 |
|---|---|
| 무료 컴플라이언스 진단 | 상담 신청 폼 |
| 서비스 안내 | `speciai.team` 서비스 소개 |
| 1:1 상담 문의 | 카카오톡 채널 |

> 위젯은 **가로 170px을 넘으면 사이드바 밖으로 잘립니다.** 세로는 자유롭습니다.

---

## 5. 추가로 준비하셔야 할 사진

지금 만든 건 전부 **그래픽**입니다. 신뢰가 필요한 업종이라 실물 사진이 몇 장 있으면 체감이 크게 달라집니다.

| 무엇 | 규격 | 어디에 쓰나 | 우선순위 |
|---|---|---|---|
| 대표/컨설턴트 인물 사진 | 정사각 1000×1000 이상 | 프로필, 소개 글, 카드뉴스 듀오톤 | **높음** — 전문직 신뢰의 핵심 |
| 상담/미팅 장면 | 가로 1300×885 | 서비스 소개 글 대표 이미지 | 높음 |
| 사무실·업무 환경 | 가로 1300×885 | 회사 소개 글 | 중간 |
| 자료·문서 클로즈업 | 가로 1300×885 | 법률·세무 글 대표 이미지 | 중간 |

**촬영할 때 지켜야 할 것**

- **가로로 찍습니다.** 세로 사진은 블로그 본문에서 화면을 다 잡아먹습니다.
- 인물은 **여백을 넉넉히** 두고 찍어야 정사각·가로 어느 쪽으로도 잘라 쓸 수 있습니다.
- 조명은 **한쪽 방향**으로 통일합니다. 카드뉴스 듀오톤 처리(`_공용/design/design-system/spec.md`)에 그대로 넘길 수 있습니다.
- 배경은 단색이나 흐린 실내가 좋습니다. 배경이 복잡하면 오렌지 브랜드 컬러가 죽습니다.

스톡 사진을 쓰신다면 **Unsplash·Pexels** 는 상업적 이용이 되지만, 법률·세무 업종은 "외국인 정장 인물 스톡"이 티가 많이 나서 오히려 신뢰를 깎습니다. 인물은 실물로 가시는 걸 권합니다.

---

## 6. 이미지 다시 만들기

문구나 색을 바꾸시려면 `render.mjs` 위쪽만 고치면 됩니다.

```bash
cd ~/Work/speciai/speciai.team/blog

node render.mjs          # 10장 전부
node render.mjs 타이틀    # 이름에 '타이틀' 들어간 것만
node render.mjs 위젯      # 위젯 배너 3종만
```

- 색은 파일 맨 위 `const C = {...}` 에 모여 있습니다.
- 문구는 각 템플릿의 `html` 안에 그대로 적혀 있습니다.
- 썸네일 템플릿(`08`)은 카테고리 뱃지·제목·부제만 갈아끼우면 글마다 재사용됩니다.

브랜드 컬러는 카드뉴스와 같은 값을 씁니다 — 메인 `#D85A30`, 보조 `#CB603D`, 배경 `#0A0A0A`.
바꾸실 거면 `_공용/design/design-system/spec.md` 쪽도 같이 봐야 합니다. 밈릴스 듀오톤이 이 색을 씁니다.
